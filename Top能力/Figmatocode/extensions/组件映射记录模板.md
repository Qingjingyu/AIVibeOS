# 组件映射记录模板

每次做组件映射时，都建议复制这份模板填写。

## 基本信息

- Figma 组件名：
- Figma 节点或页面：
- file key：
- node id：
- 对应代码组件名：
- 对应代码路径：

## 本次判断

- 本次结论是：直接映射 / 扩展已有组件 / 新增组件
- 为什么这么判断：
- 是否已经跨越职责边界：

## 结构判断

- 结构是否基本一致：
- 哪些层级一致：
- 哪些层级不同：
- 是否存在插槽差异：

## 行为判断

- 交互是否一致：
- 状态是否一致：
- 是否只是样式差异：
- 是否需要新增状态：

## 映射细节

- `variant` 映射：
- `size` 映射：
- `state` 映射：
- `icon` / `slot` 映射：
- 哪些 props 需要新增：

## 当前差异

- 与 Figma 仍有差异的地方：
- 这些差异是否影响复用：
- 后续是否需要继续补：

## 下一步动作

- [ ] 直接复用现有组件
- [ ] 补充 variant
- [ ] 补充 state
- [ ] 补充 slot
- [ ] 新增组件
- [ ] 暂不抽象，继续观察
