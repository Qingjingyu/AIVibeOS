---
paths:
  - "**/*.md"
  - "commands/*.md"
  - "skills/**/SKILL.md"
---

# Markdown Governance

- Prefer concise Chinese unless the file is already English.
- Keep headings short and purpose-driven.
- When adding a new system document, update the nearest index or entrypoint.
- When changing workflow logic, keep `commands/`, `skills/`, and `开发过程/` aligned.
- Do not leave tool adapter files pointing to missing commands or skills.
