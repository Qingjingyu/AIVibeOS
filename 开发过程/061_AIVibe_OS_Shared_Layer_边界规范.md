# AIVibe OS Shared Layer 边界规范

## 目标

定义 `packages/shared` 到底能放什么、不能放什么。

## 允许放

- 共享类型
- 常量
- 错误码
- 无业务副作用的纯函数
- 前后端都要用到的协议定义

## 不允许放

- 页面组件
- 业务流程逻辑
- 模型调用逻辑
- 数据库读写
- 只服务某个单独应用的私有工具

## 判断标准

如果一个东西离开具体业务后仍然成立，才考虑进入 shared。

## 一句话铁规矩

**shared 是“跨应用通用层”，不是“现在不知道放哪就先放这”的垃圾桶。**
