# AIVibe OS Provider 适配规范

## 目标

定义新模型厂商接入时必须满足的最低要求。

## 必须统一的能力

- 文本生成调用入口
- 超时控制
- 重试能力
- 错误结构统一
- token 与成本记录

## 不允许的做法

- 让业务代码直接感知厂商 SDK
- 不记录模型版本
- 错误结构每家都不一样

## 一句话铁规矩

**provider 适配层的任务是抹平差异，不是把差异继续暴露给业务。**
