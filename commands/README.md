# AIVibe Commands

这里放 AIVibe OS 的命令入口。

它们不是聊天装饰，而是把任务送进正确 Skill 和验证链路的入口。

## 当前命令

- `/spec`
- `/plan`
- `/build`
- `/test`
- `/review`
- `/code-simplify`
- `/ship`

## 使用原则

1. 先选阶段，再选命令。
2. 命令会指向默认 Skill，不等于你可以跳过验证。
3. 如果命令对应的前置输入不完整，先补输入，不要硬做。
